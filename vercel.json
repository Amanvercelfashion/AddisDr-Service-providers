{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && mkdir -p ../backend/node_modules/__frontend && cp -r dist/* ../backend/node_modules/__frontend/",
  "builds": [
    {
      "src": "backend/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/server.js" },
    { "src": "/(.*)", "dest": "backend/server.js" }
  ]
}
