# ── API URL ───────────────────────────────────────────────────────────────────
# Local development: leave empty (Vite proxy handles /api → localhost:4000)
# Production (Vercel): set to your deployed backend URL
VITE_API_URL=
# VITE_API_URL=https://servicesaas-api.vercel.app
